<!DOCTYPE html>
<html>
<head>
  <title>Meus Documentos</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <h1>Meus Documentos</h1>
  <a href="/editor" class="btn">Novo Documento</a>
  
  <ul>
    <% documents.forEach(doc => { %>
      <li>
        <h3><%= doc.title %></h3>
        <p>Atualizado em: <%= doc.updated_at.toLocaleString() %></p>
        <a href="/editor/<%= doc.id %>">Editar</a>
      </li>
    <% }); %>
  </ul>
</body>
</html>